# AgiAgentIskra/.env.example - Environment variables for TELOS-Delta PoC

# --- Database Configuration (pgvector) ---
# Connection string for PostgreSQL with pgvector extension enabled
DB_DSN=postgresql://user:password@host:port/database_name

# --- Data and Model Configuration ---
# Directory where raw data files for ingestion are stored
DATA_DIR=/data/raw_documents

# Embedding model name (e.g., 'e5-large-v2' or a custom 384d model)
EMBEDDING_MODEL_NAME=telos-delta-embed-384d

# --- vLLM Serving Configuration (for Phase-0 LLM) ---
# Endpoint for the vLLM service (e.g., running a 7B LLM)
VLLM_API_BASE=http://localhost:8000/v1
VLLM_MODEL_NAME=telos-delta-7b-mamba-moe

# --- Orchestration Configuration ---
# Path to the CD-Index report for Canon Review gate checks
CANON_CD_INDEX_PATH=../SpaceCoreIskra-vOmega/reports/cd-index_v0.json

